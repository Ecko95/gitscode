# p1-C1-T3-s2 — readBudgetUsage → SQL aggregate + 60 s memo

Task: T3 · Lane: C1 (after s1) · Model: opus-4-8/high
Status: pending

## Brief (Fable)

- **Goal:** replace `readBudgetUsage`'s full projection snapshot (10 `findAll` + decode per call) with a SQL aggregate over `projection_thread_activities WHERE kind IN ('usage.cost.updated','context-window.updated')`, memoized 60 s using the existing `GitsSlotScheduler` memo pattern.
- **Read first:** `apps/server/src/gits/Layers/AutomodeSupervisor.ts:429-438`, `AutomodeUsageMeter.ts:68-76`, `GitsSlotScheduler.ts:40,453-459` (memo pattern to reuse); co-located tests; plan §T3.
- **Invariants:** budget numbers must equal the snapshot-derived values on the same data (this is billing/limits accounting — correctness first); memo TTL must not let automode overshoot a budget by more than one 60 s window.
- **Acceptance:** no `getSnapshot()` in the budget path; equivalence test seeded snapshot-vs-aggregate; overshoot bound documented in Hand-back; guard green.
- **Verification (guard):** `bun x turbo run typecheck build --filter=t3` + `bun run --cwd apps/server test -- Automode`.

## Hand-back (execution agent)

_(files read · files changed · diff-stat · summary ≤1 para · ponytail shortcuts · residual risks · next steps)_

## Guard result

—

## Merge

—

## Verify result (end gate)

—

## Decision (Fable)

—
