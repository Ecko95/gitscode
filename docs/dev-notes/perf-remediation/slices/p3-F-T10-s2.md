# p3-F-T10-s2 — inactive session cap 128→16 + clear on exit

Task: T10 · Lane: F (after s1) · Model: sonnet-4-6/medium
Status: done

## Brief (Fable)

- **Goal:** drop `DEFAULT_MAX_RETAINED_INACTIVE_SESSIONS` from 128 to 16 and clear the in-memory `history` when a session exits (reload from disk on reattach — persistence already exists at `:1172`).
- **Read first:** `apps/server/src/terminal/Layers/Manager.ts:53,:57`, session-exit path, the reattach path that would reload persisted history; s1 diff (merged); plan §T10.
- **Invariants:** reattach to a recently exited session still shows scrollback (from disk); active sessions unaffected.
- **Acceptance:** exit → in-memory history freed (test observes memory-side structure emptied); reattach → history restored from disk; guard green.
- **Verification (guard):** `bun x turbo run typecheck build --filter=t3` + `bun run --cwd apps/server test -- Manager`.

## Hand-back (execution agent)

Files read: `Manager.ts`, `Manager.test.ts`.

Files changed: `apps/server/src/terminal/Layers/Manager.ts`, `apps/server/src/terminal/Layers/Manager.test.ts`.

Diff-stat: 4 hunks in Manager.ts (+15 -2), 1 hunk in Manager.test.ts (+37).

Summary: Changed `DEFAULT_MAX_RETAINED_INACTIVE_SESSIONS` 128→16. Added `session.history = ""; session.historyLineCount = 0` in both `drainProcessEvents` exit branch and `stopProcess`. Added reload-from-disk in `openOrAttachForStream` for exited/error sessions with empty history (`Effect.catch` to suppress `TerminalHistoryError`). Fixed a latent bug: `closeSession` was persisting `session.value.history` after `stopProcess` cleared it — captured history before the stop call. Added test verifying exit clears in-memory history and reattach via `attachStream` restores it from disk.

Ponytail shortcuts: none material.

Residual risks: The reload in `openOrAttachForStream` only triggers when `session.history === ""` — a session that had a legitimately empty history and is in exited state will do a redundant disk read. Acceptable: exited session with empty history is unusual and the disk read is O(file size).

## Guard result

typecheck+build: 10/10 green (no errors, suggestions only).
Manager tests: 118/118 green (was 117 + 1 new = 118).

## Merge

Commit: see git log.

## Verify result (end gate)

—

## Decision (Fable)

—
