# p3-F-T10-s2 — inactive session cap 128→16 + clear on exit

Task: T10 · Lane: F (after s1) · Model: sonnet-4-6/medium
Status: pending

## Brief (Fable)

- **Goal:** drop `DEFAULT_MAX_RETAINED_INACTIVE_SESSIONS` from 128 to 16 and clear the in-memory `history` when a session exits (reload from disk on reattach — persistence already exists at `:1172`).
- **Read first:** `apps/server/src/terminal/Layers/Manager.ts:53,:57`, session-exit path, the reattach path that would reload persisted history; s1 diff (merged); plan §T10.
- **Invariants:** reattach to a recently exited session still shows scrollback (from disk); active sessions unaffected.
- **Acceptance:** exit → in-memory history freed (test observes memory-side structure emptied); reattach → history restored from disk; guard green.
- **Verification (guard):** `bun x turbo run typecheck build --filter=t3` + `bun run --cwd apps/server test -- Manager`.

## Hand-back (execution agent)

_(files read · files changed · diff-stat · summary ≤1 para · ponytail shortcuts · residual risks · next steps)_

## Guard result

—

## Merge

—

## Verify result (end gate)

—

## Decision (Fable)

—
