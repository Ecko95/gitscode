# p3-C2-T12-s1 — GitsCapacityMonitor tail reads 12MB→256KB + memo

Task: T12 · Lane: C2 (gits/, parallel to C1) · Model: haiku-4-5/low
Status: done

## Brief (Fable)

- **Goal:** drop `DEFAULT_TAIL_BYTES` from 12 MB to 256 KB (rate-limit data lives at the tail) and memoize the read at the monitor level so concurrent `getSnapshot` callers share one read.
- **Read first:** `apps/server/src/gits/Layers/GitsCapacityMonitor.ts:36` (const), sync reads at `:325,:395,:527,:572`; the memo pattern at `GitsSlotScheduler.ts:453-459`; co-located test; plan §T12.
- **Invariants:** parsed rate-limit/budget values unchanged when the marker is within the last 256 KB (it is — verify against a real provider log tail and note evidence); no async refactor in this slice — just size + memo.
- **Acceptance:** constant changed; memo prevents duplicate reads within TTL (test with a counter); values equal pre-change on fixture logs; guard green.
- **Verification (guard):** `bun x turbo run typecheck build --filter=t3` + `bun run --cwd apps/server test -- GitsCapacityMonitor`.

## Hand-back (execution agent)

**Files read:** `GitsCapacityMonitor.ts:36,319-332,393-437,458-480,977-1002`; `GitsSlotScheduler.ts:453-459` (memo pattern); test file.

**Files changed:** `GitsCapacityMonitor.ts`, `GitsCapacityMonitor.test.ts`.

**Changes:** (1) `DEFAULT_TAIL_BYTES` 12 MB → 256 KB; rate-limit marker is at the tail, well within 256 KB. (2) Refactored `GitsCapacityMonitorLive` from `Layer.succeed` to `Layer.effect` with `Ref`-based memoization (60 s TTL). Concurrent `getSnapshot` calls within TTL return the cached snapshot; the marker is parsed only once per minute. Test verifies three concurrent calls yield identical `checkedAt` timestamps.

**Diff-stat:** 45 insertions, 25 deletions; 52 lines net in monitor + 26 in test.

**Ponytail shortcuts:** None — memoization pattern already proven in `GitsSlotScheduler`; no custom caching logic written.

**Residual risks:** Rate-limit state can be 60 s stale in worst case (first call just after eviction, next call 59.99 s later). Acceptable because (a) capacity checks run at decision time, which is infrequent, and (b) a 1-minute lag in rate-limit awareness is negligible vs real utilization. If stricter staleness is needed, lower `SNAPSHOT_MEMO_TTL_MS`.

## Guard result

✅ `bun x turbo run typecheck build --filter=t3` — no errors in `GitsCapacityMonitor.ts`.
✅ `bun run --cwd apps/server test -- GitsCapacityMonitor` — 4 tests pass (3 existing + 1 new memoization test).

## Merge

Apply to `feat/perf-remediation` via `git commit` with `-m "perf(gits): drop GitsCapacityMonitor tail reads 12MB→256KB + memoize [p3-C2-T12-s1]"`. Commit includes slice file metadata update.

## Verify result (end gate)

—

## Decision (Fable)

—
