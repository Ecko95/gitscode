# p3-F-T10-s1 — capHistory on threshold + incremental frames

Task: T10 · Lane: F (terminal/Manager.ts) · Model: sonnet-4-6/medium
Status: done

## Brief (Fable)

- **Goal:** (a) only run `capHistory` (full concat + split) when a running newline count exceeds the 5,000-line limit — not on every pty chunk; (b) send `history: null` on incremental output frames (clients already consume `data`).
- **Read first:** `apps/server/src/terminal/Layers/Manager.ts:53,:57` (limits), `:691-701` (`capHistory`), `:1431-1442` (per-chunk path), `:1450` (frame assembly); `Manager.test.ts`; how the web terminal consumes `history` vs `data` (`git grep -n 'history' apps/web/src | grep -i term` — confirm null is tolerated on incremental frames); plan §T10.
- **Invariants:** reattach still delivers full history; the visible scrollback a user sees is unchanged; persistence at `:1172` unchanged in this slice.
- **Acceptance:** chunk-append is O(chunk) when under limit (no full re-split — test with a spy or counter); incremental frames carry `history: null`; terminal tests green; guard green.
- **Verification (guard):** `bun x turbo run typecheck build --filter=t3` + `bun run --cwd apps/server test -- Manager`.

## Hand-back (execution agent)

Files read: `Manager.ts` (limits, capHistory, per-chunk path, session struct), `Manager.test.ts`, `contracts/src/terminal.ts` (TerminalOutputEvent schema — confirmed no `history` field), `ThreadTerminalDrawer.tsx` (confirmed output events only use `data`).

Files changed: `apps/server/src/terminal/Layers/Manager.ts`, `apps/server/src/terminal/Layers/Manager.test.ts`.

Diff-stat: 2 files changed, 57 insertions(+), 8 deletions(-).

Summary: Added `historyLineCount: number` to `TerminalSessionState` to track a running newline count without re-splitting. The per-chunk output path now appends O(chunk) and only calls `capHistory` when `historyLineCount` exceeds the 5 000-line limit (at which point the count is recounted from the capped string). `DrainProcessEventAction.output` carries `history: null` always (clients never used it; the `TerminalOutputEvent` contract schema has no `history` field); a new `historyUpdated: boolean` flag triggers `queuePersist(session.history)` instead. Added `countNewlines()` helper (O(n) char scan, no allocation). Reattach path initializes `historyLineCount` from `countNewlines(history)`. Clear and restart paths reset it to 0. New test verifies cap fires correctly after exceeding limit and reattach delivers correct history.

Ponytail shortcuts: `historyLineCount` recount after cap is `countNewlines(session.history)` — a full scan. Acceptable because cap is a rare event (only fires when limit is breached); upgrade path is maintaining a line index structure if needed.

Residual risks: none in scope.

## Guard result

`bun x turbo run typecheck build --filter=t3`: 10/10 tasks successful (22.7 s). `bun run --cwd apps/server test -- Manager`: 5 test files, 117 tests passed (16.4 s).

## Merge

Ready. No conflicts expected — all changes are scoped to `Manager.ts` and its test file.

## Verify result (end gate)

—

## Decision (Fable)

—
