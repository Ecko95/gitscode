{
  "commands": [
    {
      "id": "web-dev",
      "name": "Web dev",
      "description": "Start the main GITS web app and publish it on the tailnet.",
      "command": "bun run --filter=@t3tools/web dev -- --host 127.0.0.1 --port 3000",
      "port": 3000,
      "host": "127.0.0.1",
      "publishOnTailnet": true,
      "servePort": 3000
    },
    {
      "id": "marketing-dev",
      "name": "Marketing dev",
      "description": "Start the marketing site and publish it on the tailnet.",
      "command": "bun run --filter=@t3tools/marketing dev -- --host 127.0.0.1 --port 4321",
      "port": 4321,
      "host": "127.0.0.1",
      "publishOnTailnet": true,
      "servePort": 4321
    },
    {
      "id": "server-dev",
      "name": "Server dev",
      "description": "Run the local Node server in watch mode.",
      "command": "bun run --filter=t3 dev",
      "publishOnTailnet": false
    }
  ]
}
